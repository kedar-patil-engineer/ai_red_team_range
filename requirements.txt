# Core engine (runs today on Python 3.14)
pyyaml>=6.0.0
python-dotenv>=1.0.1
requests>=2.32.0
rich>=13.7.0
tabulate>=0.9.0

# Targets
# 2.x is required for compatibility with modern httpx (older versions pass a
# removed 'proxies' argument and crash on httpx >= 0.28).
openai>=2.0.0

# Reporting and serving
reportlab>=4.0.0
fastapi
uvicorn[standard]
streamlit>=1.37.0

# Testing
pytest>=8.0.0

# ---------------------------------------------------------------------------
# Optional external red team engines (install in a separate Python 3.11 venv).
# Garak and PyRIT lag new Python releases and may not import on 3.14 yet.
# See requirements-integrations.txt and docs/INTEGRATIONS.md.
# ---------------------------------------------------------------------------
